# 部署配置

server:
  host: 0.0.0.0
  port: 8001
  workers: 1
  reload: false

milvus:
  host: localhost
  port: 19530
  collection: prospectus_chunks

logging:
  level: INFO
  file: logs/app.log
  max_bytes: 10485760
  backup_count: 5

paths:
  data_dir: data/
  upload_dir: data/uploads/
  processed_dir: data/processed/
